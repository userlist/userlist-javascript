import { expect } from 'chai';

import { Widget, Channel } from '../src/index.js';

describe('#Widget', function () {
  it('should exist', function () {
    expect(Widget).exist;
  });
});

describe('#Channel', function () {
  it('should exist', function () {
    expect(Channel).exist;
  });
});
